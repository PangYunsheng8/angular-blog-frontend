import { ComputeTagsPipe } from './compute-tags.pipe';

describe('ComputeTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new ComputeTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
